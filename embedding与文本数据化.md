# embedding与文本数据化

## 文本数据化

早期计算机无法直接“理解”文字。

人类语言必须转化为机器能处理的形式 —— 符号、索引、离散标识符。

### 独热编码

独热编码是一种把离散的符号变量（例如单词、类别）转化为数值向量的方式。

![Alt text](image.png)

这样就可以表示这个词有没有在这句话中出现过。

虽然简单直观，但它有 严重缺陷：

![Alt text](image-1.png)

### 词袋模型

每个文档被表示为词频统计向量：

$$doc=[count(word_1),count(word_2)]$$

这里会忽略掉词序和上下文，只关心出现次数。

### 发展时间线

![Alt text](image-2.png)

这里会忽略掉词序和上下文，只关心出现次数。

## Embedding

Embedding（嵌入）就是把“离散的符号”映射到一个连续的稠密向量空间中，使得“语义相似”的对象在向量空间中距离更近。

例如：

![Alt text](image-3.png)

Embedding的诞生解决了独热编码矩阵过于稀疏以及词袋模型不包含语义信息的问题。

### 作用

1. 在 embedding 空间中查找最近邻，这可以很好的用于根据用户的兴趣来进行推荐。
2. 作为监督性学习任务的输入。
3. 用于可视化不同离散变量之间的关系。

### 原理分析

假设有矩阵 $E\in R^{n \times m}$ Embedding做的事情是，给定索引i获得矩阵E中的第i行$e_i\in R^{1 \times m} $这个操作可以使用矩阵乘法来表示。

![Alt text](image-6.png)

举例说明：

![Alt text](image-7.png)

推广到多个索引的情况：

![Alt text](image-8.png)

这步操作就是将一个由one-hot编码组成的稀疏矩借助Embedding阵转化为一个稠密矩阵的过程。这里的Embedding相当于一个存储了很多个词的仓库，而我们所做的事情就是通过one-hot从这个仓库中去找到我们所需要的词的Embedding。在实际的操作中，这一步并不会使用矩阵乘法，而是改为了一个查询的操作。

#### 空间映射表示

![Alt text](image-5.png)

### 语义学习过程

Embedding 向量不是人工定义的，而是通过训练自动学出来的。通过各种目标函数，让语义相似的词“靠近”，语义不同的词“远离”。
可以看到在向量空间中，语义相似的词会自动聚类到相近区域。

### 类型与应用

![Alt text](image-4.png)