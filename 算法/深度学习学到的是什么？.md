# 深度学习学到的是什么？

深度学习的本质就是通过大量数据和优化算法，学习出一组能够最小化预测误差的参数（权重和偏置），从而形成一个对输入到输出的非线性映射模型。

## 权重和偏置

以最简单的线性模型为例：

$$y=Wx+b$$

其中：

* $x$：输入向量（比如二维坐标 [x₁, x₂]）
* $W$：权重矩阵（决定输入如何线性组合）
* $b$：偏置（控制整体平移）
* $y$：输出结果

这其实就是一个仿射变换（Affine Transformation）。

* 权重 (W)：控制每个输入对输出的“影响程度”。

   比如：如果 (W = [2, -1])，说明输入的第一个分量会让输出变大两倍，第二个分量会让输出减少一点。
* 偏置 (b)：在所有输入为 0 时输出的“基线值”。它就像函数图像的 y 轴截距，能让模型更灵活地移动决策边界。

## 为什么「有了权重和偏置」就能推理？

因为一旦模型学习完毕，这些参数实际上已经编码了输入→输出之间的数学映射规律。
推理时的过程就是：

1. 把输入 (x) 代入；

2. 依次经过各层的：

3. 最终得到输出 。

整个过程不再需要学习，而只是按已知公式计算。

模型推理 ≈ 已学函数的前向传播。

### 从几何角度理解

假设你要做一个分类问题，比如区分红点和蓝点。

第一步：线性模型

* 权重 (W) 控制超平面的“方向”（即决策边界的斜率）；
* 偏置 (b) 控制超平面的位置（即决策边界的平移）；

$$W \cdot x + b = 0$$

就是分界线。

训练时，模型调整 (W, b)，直到分界线最能区分红点和蓝点。

第二步：多层神经网络（非线性）

多个线性变换 + 激活函数堆叠后，
 
模型能学出非常复杂的、弯曲的、非线性的边界。而这些复杂形状的「弯曲决策边界」，
其实就是由一组数值化的参数共同决定的。

总结:

|概念	|含义|	推理时的作用|
|---|---|---|
权重 (W)|	控制输入特征的影响方向与强度|	决定每个输入对结果的贡献|
偏置 (b)|	决定输出的整体偏移|	允许决策边界自由平移|
推理 (Inference)|	前向传播|	把输入代入训练好的映射函数中求结果|

可以这样理解：

* 训练是「找出规律」的过程（通过调整 W 和 b），
* 推理是「套用规律」的过程（使用固定的 W 和 b 计算）。

## 模型文件的结构

无论框架如何，模型文件一般包含以下几类信息：

**模型权重（Weights / Parameters）**

神经网络所有可训练参数（权重矩阵、偏置向量等）。

常见命名：state_dict（PyTorch）、variables（TF）。

**模型结构或配置（Architecture / Config）**

描述网络拓扑（层次、超参数、hidden size、num_layers 等）。

有时以 config.json、model_config 等形式单独保存；有时在代码里以类定义表示（需代码同步）。

**预处理器 / tokenizer / vocabulary**

文本模型需要 tokenizer（词表、merges、tokenizer config）、图像模型有预处理尺度/均值/方差。

Hugging Face：tokenizer.json、vocab.txt、tokenizer_config.json 等。

**推理签名 / 接口定义（Signatures）**:指明如何调用模型（输入输出名称、shape、dtype），便于部署（SavedModel 的 signature，ONNX 的 input/output names）。

**优化器/训练状态（可选）**:optimizer state、epoch、global_step、learning_rate 等，主要用于恢复训练。

**元数据 / 文档（Metadata）**:训练时用的数据集、预处理步骤、作者、license、模型卡（model card）等，有助于可追溯与合规。

**分片 / 量化 / 编译产物**

大模型常把权重分片（shard）成多个文件（如 pytorch_model.bin.index.json / pytorch_model-00001-of-00008.bin）。
量化后可能有 int8 权重、校准表等。

有的会包含转换后的推理格式（TorchScript、TFLite、TensorRT、ONNX 等）。
