# 神经网络

神经网络是一种模仿人类大脑神经元连接方式的计算模型。

它的本质是一个非线性函数逼近器，通过训练（大量数据 + 反向传播算法），来学习从输入到输出的复杂映射关系。

通俗说：神经网络就是一个能「自动学习输入与输出之间规律」的系统。

## 神经网络的结构

一个典型的神经网络由以下几层构成：

>输入层 → 隐藏层（一个或多个） → 输出层

每一层都由若干个“神经元（Neuron）”组成，每个神经元与前一层的所有神经元相连。

这里我们构建一个两层神经网络，理论上两层神经网络已经可以拟合任意函数。这个神经网络的结构如下图：

![Alt text](/images/神经网络/image.png)

这里我们举一个例子来说明每一层要做什么。

如下图，我们已知四个数据点(1,1)(-1,1)(-1,-1)(1,-1)，这四个点分别对应I~IV象限，如果这时候给我们一个新的坐标点（比如(2,2)），那么它应该属于哪个象限呢？

![Alt text](/images/神经网络/image-1.png)

### 输入层

在我们的例子中，输入层是坐标值，例如（1,1），这是一个包含两个元素的数组，也可以看作是一个1*2的矩阵。输入层的元素维度与输入量的特征息息相关，如果输入的是一张32*32像素的灰度图像，那么输入层的维度就是32*32。

### 从输入层到隐藏层

连接输入层和隐藏层的是W1和b1。由X计算得到H十分简单，就是矩阵运算：

$$H=x \times W_1+b_1$$

如上图中所示，在设定隐藏层为50维（也可以理解成50个神经元）之后，矩阵H的大小为（1*50）的矩阵。
这个过程可以进行如下的拆解：

$$X = [x,y]$$

$$W_1 =
\begin{bmatrix}
w_{11} & w_{12} & w_{13} & ... & w_{1,50} \\
w_{21} & w_{22} & w_{23} & ... & w_{2,50}
\end{bmatrix}$$

$$b_1 = [b_1, b_2, ..., b_{50}]$$

则隐藏层第 𝑗 个神经元的计算方式是：

也就是说：
* 每个隐藏层神经元都“连接”输入层的两个特征；
* 它会对这两个特征分别乘以不同的权重；
* 然后加上偏置 $b_j$ ，最后通过激活函数$f$。
  
这里的W1是从哪里来的？

这里的W1代表一组权重，在最初的时候W1是随机初始化的。在后续的训练过程中会通过梯度下降法来不停的优化W1的矩阵。

从隐藏层到输出层

连接隐藏层和输出层的是W2和b2。同样是通过矩阵运算进行的：

$$Y=H \times W_2+b_2$$

## 每个神经元的结构

一个神经元就像一个小计算单元：

>输入向量 x → 线性加权 → 激活函数 → 输出

* $x$：输入向量（例如一张图片的像素）
* $W$：权重矩阵（表示神经元间连接强度）
* $b$：偏置项（调整激活阈值）
* $f$：激活函数（让模型学非线性关系，比如 ReLU、Sigmoid）
* $y$：神经元的输出

## 激活函数

### 为什么需要激活函数？

#### 1. 引入非线性

没有激活函数​​：神经网络只是​​线性变换的堆叠​​（矩阵乘法），无论多少层都等价于单层线性模型：

输出 = $W3(W2(W1*X + b1) + b2) + b3 = W'*X + b'$

​​有激活函数​​：每层通过非线性函数转换，使网络能够​​拟合任意复杂函数​​（万能逼近定理）。

激活函数是一个非线性函数（比如 sigmoid、ReLU、tanh），它让神经元的输出不再只是线性组合。

我们加上激活函数：

$$y=W_2f(W_1x)$$

画个图来表示就是图中这样：

![Alt text](/images/神经网络/image-2.png)

这种非线性弯曲让每一层的输出可以重新映射输入空间，于是：

* 第一层：对原始数据做“方向敏感”的线性变换 + 非线性变换；

* 第二层：在第一层的特征基础上再做更复杂的组合；

* 第三层：进一步组合抽象特征，形成更高层的语义。

#### 2. 提供梯度信号，让模型能学习

在反向传播中，我们通过链式法则计算梯度：

如果没有激活函数，每层都是线性关系，梯度传播就会退化、难以优化。

对梯度传播的影响：

* 所有层的梯度都线性相关；

* 没有“非线性压缩/放大”作用；

* 导致梯度容易变得非常小（消失）或非常大（爆炸）；

* 优化器几乎无法区分不同层的作用。

非线性激活函数使得梯度在各层之间有区别性，使模型能够逐层调整参数来学习复杂特征。

### 激活函数有哪些？

![Alt text](/images/神经网络/image-3.png)

为什么 ReLU 常用于神经网络的激活函数？

1. 在前向传播和反向传播过程中，ReLU 相比于 Sigmoid 等激活函数计算量小；

2. 避免梯度消失问题。对于深层网络， Sigmoid 函数反向传播时，很容易就会出现梯度消失问题（在 Sigmoid
接近饱和区时，变换太缓慢，导数趋于 0，这种情况会造成信息丢失），从而无法完成深层网络的训练。

3. 可以缓解过拟合问题的发生。Relu 会使一部分神经元的输出为 0，这样就造成了网络的稀疏性，并且
减少了参数的相互依存关系，缓解了过拟合问题的发生。

## 神经网络的处理过程

### 前向传播（Forward Propagation）

数据从输入层依次传到隐藏层、再传到输出层。
在每一层，计算如下：

$$a^{(l)} = f(W^{(l)} a^{(l-1)} + b^{(l)})$$

其中：
* $a^{(l-1)}$：上一层输出
* $W^{(l)}$：当前层的权重
* $b^{(l)}$：当前层的偏置
* $f$：激活函数

最终在输出层得到预测结果。

### 损失计算（Loss Function）

模型把预测结果与真实标签进行对比，比如真实是“猫”，

则使用交叉熵损失函数计算误差：

$$L = -\sum y_{true} \log(y_{pred})$$

对于非分类问题也有其他的损失函数可以对预测的结果进行计算。

### 反向传播（Backpropagation）

核心过程：模型自动计算每个参数对损失的影响（梯度）。通过链式法则（chain rule），逐层反向传播误差：

$$\frac{\partial L}{\partial W^{(l)}} = \frac{\partial L}{\partial a^{(l)}} \cdot \frac{\partial a^{(l)}}{\partial W^{(l)}}$$

这里为什么可以用链式法则呢？

因为多层神经网络运算的本质其实就是多个函数嵌套的运算。满足 $h'(x) = f'(g(x)) · g'(x)​$ 的数学原理。

### 参数更新（Gradient Descent）

使用梯度下降算法更新参数：

$$W^{(l)} \leftarrow W^{(l)} - \eta \cdot \frac{\partial L}{\partial W^{(l)}}$$
